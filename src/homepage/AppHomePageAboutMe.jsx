'use client'

import { useIntersectionObserver } from '@/context/GlobalContext';
import Image from 'next/image';

export default function AppHomePageAboutMe() {

    return (
        <div>
            <h2>homepage</h2>
        </div>
    )
}